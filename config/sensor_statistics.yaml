- platform: history_stats
  name: daily_away_christian
  entity_id: device_tracker.mobile_christian
  state: 'not_home'
  type: time
  start: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'
  end: '{{ now() }}'
- platform: history_stats
  name: daily_away_adriana
  entity_id: device_tracker.mobile_adriana
  state: 'not_home'
  type: time
  start: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'
  end: '{{ now() }}'
- platform: statistics
  entity_id: sensor.evoila_house
  name: evoila_house_stat
- platform: statistics
  entity_id: sensor.house_evoila
  name: house_evoila_stat