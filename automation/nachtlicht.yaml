- id: nightstand_lamp_adriana
  alias: Nachtlicht Adriana
  action:
  - service: light.toggle
    entity_id: light.adri_nachtlicht
  trigger:
  - platform: event
    event_type: click
    event_data:
      entity_id: binary_sensor.switch_nightstand_adriana
      click_type: single
- id: nightstand_lamp_christian
  alias: Nachtlicht Christian
  action:
  - service: light.toggle
    entity_id: light.christian_nachtlicht
  trigger:
  - platform: event
    event_type: click
    event_data:
      entity_id: binary_sensor.switch_nightstand_christian
      click_type: single